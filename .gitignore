.venv/
__pycache__/
*.pyc
_site/
# data/files/ holds the committed sample for the `local` source (so CI has
# something to export). Drop your own .trd/.tap there to grow the local mirror.

# sp3 detail-page crawl cache (SP3_CACHE); CI keeps its copy in actions/cache
.cache/
